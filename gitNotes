git notes:

install git
using git cmd clone learnings repository here......
atom-add project.
add project folder-add learnings..automatic unstage..thn rit clk stge coments cmit push.

command prompt:
 documents->education->place(need to clone)
 cd documents
 cd education
 git clone paste link

Run
  git config --global user.email "devamathanika@gmail.com"
  git config --global user.name "mathanikasekar"
